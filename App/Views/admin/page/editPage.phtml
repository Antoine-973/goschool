<div class="container">
    <div class="content admin-page--edit">
        <div class="col-12">
            <h1>Éditer la page</h1>
            <?php if(isset($errors) && !empty($errors)): ?>
                <ul>
                    <?php foreach($errors as $error): ?>
                        <li><?php echo $error;?></li>
                    <?php endforeach; ?>
                </ul>
            <?php endif; ?>

            <?= $pageEdit; ?>
            <a href="/admin/pages" class="btn btn--infos btn--sm">Revenir à la liste</a>
        </div>
    </div>
</div>

<script>
    ClassicEditor
        .create( document.querySelector( '#content' ) )
        .then( editor => {
            console.log( editor );
        } )
        .catch( error => {
            console.error( error );
        } );
</script>
