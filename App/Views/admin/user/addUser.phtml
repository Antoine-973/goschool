<div class="content admin-article--add">
    <div class="container">
        <h1>Ajouter un utilisateur</h1>
        <div class="col-6">


    <?php if(isset($errors) && !empty($errors)): ?>
        <ul>
            <?php foreach($errors as $error): ?>
                <li><?php echo $error;?></li>
            <?php endforeach; ?>
        </ul>
    <?php endif; ?>

    <?= $userAdd; ?>
    <a href="/admin/users" class="btn btn--infos btn--sm">Revenir à la liste</a>
</div>
</div>
</div>