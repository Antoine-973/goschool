<div class="content admin-article--list">
    <h1>List of articles</h1>
    <div class="container">
        <div class="container col-10">
        <?php $keys = array_keys($articles);?>
        <a href="article/add" class="btn btn--infos btn--sm"><i class="fas fa-plus-circle"></i> Ajouter</a>
        <a href="article/edit" class="btn btn--warning btn--sm">Modifier</a>
            <table id="articletableid" class="display">
                    <thead>
                        <tr>
                            <?php for($i = 0; $i < 1; $i++) {
                                foreach($articles[$keys[$i]] as $key => $value) { ?>
                            <th><?php echo $key ?></th>
                            <?php } ?>
                            <?php }?>
                            <th>Modifier</th>
                            <th>Supprimer</th>
                        </tr>
                    </thead>

                <tbody>
                    <?php for($i = 0; $i < count($articles); $i++) { ?>
                        <tr>
                            <?php foreach($articles[$keys[$i]] as $key => $value) { ?>
                                <td><?php echo $value ?></td>
                            <?php } ?>
                            <td><a href="article/edit?id=<?php echo $articles[$i]['id'] ?>" class="btn btn--warning btn--sm"><i class="fas fa-edit"></i></a></td>
                            <td><a href="javascript:confirmDelete('article/delete?id=<?php echo $articles[$i]['id'] ?>')" class="btn btn--danger btn--sm"><i class="fas fa-trash-alt"></i></a></td>
                        <tr>
                        <?php } ?>
                </tbody>
                <tfoot>
                    <tr>
                        <?php for($i = 0; $i < 1; $i++) {
                            foreach($articles[$keys[$i]] as $key => $value) { ?>
                        <th><?php echo $key ?></th>
                        <?php } ?>
                        <?php }?>
                        <th>Modifier</th>
                        <th>Supprimer</th>
                    <tr>
                </tfoot>
            </table>
        </div>
    </div>
</div>

