<div class="add-form">
    <h1>Ajouter un nouveau commentaire</h1>
    <?php if(isset($errors) && !empty($errors)): ?>
        <ul>
            <?php foreach($errors as $error): ?>
                <li><?php echo $error;?></li>
            <?php endforeach; ?>
        </ul>
    <?php endif; ?>

    <?= $commentAdd; ?>
</div>
<div class="back-to-list">  <a href="/admin/comments" class="btn btn--infos btn--sm">Revenir à la liste</a></div>